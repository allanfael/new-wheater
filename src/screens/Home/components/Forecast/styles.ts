import styled from 'styled-components/native';

import size, { marginBottom } from '@utils/size';

export const Container = styled.View`
  margin-top: ${size(40)}px;
  margin-bottom: ${marginBottom(40)}px;
`;
