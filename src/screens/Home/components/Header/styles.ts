import styled from 'styled-components/native';
import { marginTop, marginBottom } from '@utils/size';

export const Container = styled.View`
  align-items: flex-end;
  width: 100%;
  margin-top: ${marginTop(30)}px;
`;
