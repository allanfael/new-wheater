import styled from 'styled-components/native';
import { marginTop } from '@utils/size';

export const Container = styled.View`
  align-items: flex-end;
  width: 100%;
  margin-top: ${marginTop(40)}px;
`;
